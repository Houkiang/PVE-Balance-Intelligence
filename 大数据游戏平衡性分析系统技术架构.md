# 基于大数据的多人联机 PVE 游戏平衡性分析系统技术架构

## 1. 项目概述

本系统面向多人联机、同地图、类肉鸽 PVE 无限割草游戏的平衡性分析。游戏中怪物按轮次刷新，玩家在同一房间内共同战斗，所有玩家死亡后对局失败。系统以离线分析为主，实时监控为辅，重点分析职业、地图区域、卡牌强度以及职业与卡牌流派组合。

核心目标如下：

1. 通过大数据分析发现职业、卡牌和地图设计中的强弱差异。
2. 通过离线统计和关联规则挖掘辅助游戏数值平衡调整。
3. 通过实时指标监控发现异常版本、异常地图点位或异常卡牌表现。
4. 为策划或开发人员提供可视化分析看板和调参依据。

## 2. 总体架构

系统采用分层式大数据架构，主要包括数据采集层、数据接入层、数据存储层、离线计算层、实时计算层、分析建模层和可视化展示层。

整体数据流如下：

```text
游戏服务端埋点日志
        |
        v
日志采集 / Kafka
        |
        +--------------------+
        |                    |
        v                    v
HDFS / Hive 原始数据      Flink 实时计算
        |                    |
        v                    v
Spark 离线清洗与分析     实时指标库 / 告警
        |
        v
Hive / ClickHouse / Doris
        |
        v
Superset / Grafana / Web 看板
```

## 3. 各部分技术栈与职责

### 3.1 数据采集层

主要职责：

记录游戏对局过程中的关键事件，为后续分析提供原始数据。

推荐技术栈：

| 模块 | 技术选型 | 说明 |
| --- | --- | --- |
| 游戏服务端埋点 | Java / Go / Python / C# | 根据游戏服务端语言决定 |
| 日志格式 | JSON | 课程设计中更易调试和展示 |
| 日志输出 | Filebeat / Logback / 自定义日志文件 | 将游戏事件写入文件或推送到 Kafka |
| 时间同步 | NTP | 保证多玩家事件时间顺序基本可靠 |

核心事件：

| 事件名 | 含义 |
| --- | --- |
| `battle_start` | 对局开始 |
| `battle_end` | 对局结束 |
| `player_join` | 玩家进入房间 |
| `wave_start` | 轮次开始 |
| `wave_end` | 轮次结束 |
| `enemy_spawn` | 敌人刷新 |
| `enemy_kill` | 敌人被击杀 |
| `player_death` | 玩家死亡 |
| `level_up` | 玩家升级 |
| `card_choice` | 玩家升级时看到的卡牌候选 |
| `card_pick` | 玩家选择卡牌 |
| `position_tick` | 玩家定时位置采样 |

最小运行要求：

| 项目 | 最小要求 |
| --- | --- |
| 日志格式 | JSON，每行一条事件 |
| 必须字段 | `event_type`、`event_time`、`match_id`、`room_id`、`player_id` |
| 采样频率 | 位置数据建议 1-5 秒采样一次 |
| 数据规模 | 至少 100-500 场模拟对局，用于课程展示 |
| 部署要求 | 单机即可，可先使用本地日志文件模拟游戏服务端输出 |

### 3.2 数据接入层

主要职责：

接收游戏日志，将实时事件写入消息队列，并保证原始数据可追溯。

推荐技术栈：

| 模块 | 技术选型 | 说明 |
| --- | --- | --- |
| 消息队列 | Kafka | 承接实时事件流 |
| 日志采集 | Filebeat / Flume / 自定义 Producer | 将日志文件发送到 Kafka |
| 数据通道 | Kafka Topic | 按事件类型或业务域划分 Topic |

建议 Topic 设计：

| Topic | 内容 |
| --- | --- |
| `game_battle_event` | 对局开始、结束、轮次等事件 |
| `game_player_event` | 玩家升级、死亡、位置等事件 |
| `game_card_event` | 卡牌候选、卡牌选择等事件 |
| `game_enemy_event` | 敌人刷新、击杀等事件 |

最小运行要求：

| 项目 | 最小要求 |
| --- | --- |
| Kafka | 单节点 Kafka 即可 |
| ZooKeeper / KRaft | 任选一种，课程环境推荐 Kafka 自带 KRaft 模式 |
| Topic 分区 | 每个 Topic 1-3 个分区 |
| 数据保留 | 1-7 天 |
| 替代方案 | 若部署 Kafka 成本较高，可使用本地 JSON 文件直接导入 HDFS/Hive |

### 3.3 数据存储层

主要职责：

保存原始数据、清洗后的明细数据和分析结果数据。

推荐技术栈：

| 模块 | 技术选型 | 说明 |
| --- | --- | --- |
| 分布式文件系统 | HDFS | 存储原始日志和离线中间结果 |
| 数据仓库 | Hive | 建立 ODS、DWD、DWS、ADS 分层表 |
| 元数据管理 | Hive Metastore | 管理表结构和分区 |
| OLAP 查询 | ClickHouse / Apache Doris | 面向看板的高性能聚合查询 |
| 小规模替代 | MySQL / PostgreSQL | 数据量较小时可承载 ADS 层结果表 |

数据分层设计：

| 层级 | 含义 | 示例表 |
| --- | --- | --- |
| ODS | 原始数据层 | `ods_game_event_log` |
| DWD | 明细数据层 | `dwd_player_event_detail`、`dwd_card_pick_detail` |
| DWS | 汇总数据层 | `dws_hero_balance_day`、`dws_card_strength_day` |
| ADS | 应用数据层 | `ads_hero_balance_report`、`ads_map_heatmap_report` |

最小运行要求：

| 项目 | 最小要求 |
| --- | --- |
| HDFS | 单节点伪分布式即可 |
| Hive | 本地 Metastore 或 MySQL Metastore |
| 分区字段 | 至少按 `dt` 日期分区 |
| 数据格式 | 课程展示推荐 Parquet 或 ORC，也可先用 Text/JSON |
| 存储空间 | 10GB 以上即可完成小规模演示 |

### 3.4 离线计算层

主要职责：

对历史对局数据进行清洗、聚合、统计和挖掘，形成平衡性分析指标。

推荐技术栈：

| 模块 | 技术选型 | 说明 |
| --- | --- | --- |
| 离线计算 | Spark SQL / PySpark | 适合批处理和课程展示 |
| 调度工具 | Airflow / DolphinScheduler / Cron | 定时执行离线任务 |
| 机器学习库 | Spark MLlib | 用于 FP-Growth 关联规则挖掘 |
| 数据质量 | 自定义 SQL 校验 | 检查空值、重复、异常事件 |

核心离线任务：

| 任务 | 输入 | 输出 |
| --- | --- | --- |
| 事件清洗任务 | ODS 原始日志 | DWD 标准事件明细 |
| 对局汇总任务 | DWD 对局事件 | 对局级宽表 |
| 玩家汇总任务 | DWD 玩家事件 | 玩家级表现宽表 |
| 职业平衡任务 | 玩家级宽表 | 职业平均存活轮次、每轮击杀 |
| 地图热力任务 | 位置、死亡、刷新事件 | 网格死亡频率、停留时长、刷新密度 |
| 卡牌强度任务 | 卡牌选择、对局结果 | 选取率、胜率提升、轮次提升 |
| 流派挖掘任务 | 卡牌集合、职业集合 | 常见组合、支持度、置信度、提升度 |

最小运行要求：

| 项目 | 最小要求 |
| --- | --- |
| Spark | 单机 Local 模式或 Standalone 模式 |
| CPU | 2 核以上 |
| 内存 | 4GB 以上，推荐 8GB |
| 数据量 | 千万级以内可单机演示，课程设计通常足够 |
| 执行频率 | 每日一次或手动执行 |

### 3.5 实时计算层

主要职责：

实时监控关键指标，发现异常后生成告警，作为离线分析的补充。

推荐技术栈：

| 模块 | 技术选型 | 说明 |
| --- | --- | --- |
| 实时计算 | Flink | 消费 Kafka 事件流并实时聚合 |
| 状态管理 | Flink State Backend | 保存窗口统计状态 |
| 实时结果存储 | Redis / ClickHouse / MySQL | 保存实时监控结果 |
| 告警 | 企业微信 / 邮件 / 控制台日志 | 课程设计中可用控制台或 Web 页面展示 |

实时监控指标：

| 指标 | 说明 |
| --- | --- |
| 职业实时死亡率 | 某职业短时间内死亡率异常升高 |
| 地图网格死亡激增 | 某区域死亡频率超过阈值 |
| 卡牌选取率异常 | 某卡牌选取率突然过高或过低 |
| 平均存活轮次异常 | 新版本上线后整体存活轮次明显变化 |

最小运行要求：

| 项目 | 最小要求 |
| --- | --- |
| Flink | 单机 Standalone 或 Local 模式 |
| 数据源 | Kafka 或本地 Socket/File 模拟 |
| 窗口大小 | 1 分钟、5 分钟、15 分钟 |
| 状态规模 | 小规模课程演示可使用内存状态 |
| 替代方案 | 如果时间不足，可只实现 Spark 离线分析，实时层以设计说明形式呈现 |

### 3.6 分析建模层

主要职责：

围绕游戏平衡性问题设计指标体系和分析方法。

推荐技术栈：

| 分析主题 | 技术栈 | 方法 |
| --- | --- | --- |
| 职业平衡性 | Spark SQL | 分组聚合、版本对比 |
| 地图难点区域 | Spark SQL + GIS 网格化思想 | 坐标离散化、热力统计 |
| 卡牌强度 | Spark SQL / PySpark | 分层对比、收益计算 |
| 流派组合强度 | Spark MLlib FP-Growth | 关联规则挖掘 |
| 趋势分析 | SQL + 可视化工具 | 时间序列对比 |

#### 3.6.1 职业平衡性

指标：

| 指标 | 计算说明 |
| --- | --- |
| 平均存活轮次 | 某职业玩家最终存活轮次的平均值 |
| 每轮平均击杀数 | 某职业总击杀数 / 参与轮次数 |
| 失败局占比 | 使用某职业的对局失败数量 / 总对局数量 |
| 职业使用率 | 某职业出场次数 / 总玩家出场次数 |

最小运行要求：

| 项目 | 最小要求 |
| --- | --- |
| 必要数据 | 职业选择、玩家死亡、击杀、轮次、对局结果 |
| 最小样本 | 每个职业至少 30-50 名玩家样本 |
| 输出形式 | 职业强度排行榜、版本对比表 |

#### 3.6.2 地图难点区域

指标：

| 指标 | 计算说明 |
| --- | --- |
| 单位网格死亡频率 | 网格内死亡次数 / 总死亡次数 |
| 玩家停留时长 | 玩家在网格内累计停留时间 |
| 敌人刷新密度 | 网格内敌人刷新数量 / 网格面积 |
| 危险系数 | 死亡频率、停留时长、刷新密度的综合评分 |

地图网格化方式：

```text
grid_x = floor(pos_x / grid_size)
grid_y = floor(pos_y / grid_size)
grid_id = concat(map_id, '_', grid_x, '_', grid_y)
```

最小运行要求：

| 项目 | 最小要求 |
| --- | --- |
| 必要数据 | 玩家位置、死亡位置、敌人刷新位置 |
| 位置采样 | 1-5 秒一次 |
| 网格大小 | 根据地图尺寸设置，如 5x5 或 10x10 游戏单位 |
| 输出形式 | 地图热力图、危险区域 TopN |

#### 3.6.3 卡牌强度

指标：

| 指标 | 计算说明 |
| --- | --- |
| 卡牌选取率 | 被选择次数 / 出现在候选池次数 |
| 携带后胜率提升 | 携带该卡牌玩家胜率 - 未携带该卡牌玩家胜率 |
| 携带后平均轮次提升 | 携带该卡牌平均轮次 - 未携带该卡牌平均轮次 |
| 武器成长曲线 | 武器等级与伤害、攻速、范围等属性之间的变化曲线 |

注意事项：

卡牌强度容易受到职业、玩家水平和出现轮次影响。课程设计中可采用按职业、按轮次阶段分组的方式降低偏差。

最小运行要求：

| 项目 | 最小要求 |
| --- | --- |
| 必要数据 | 卡牌候选、卡牌选择、对局结果、最终轮次 |
| 最小样本 | 每张核心卡牌至少出现 50 次以上 |
| 输出形式 | 卡牌强度排行、卡牌收益对比图、成长曲线图 |

#### 3.6.4 职业与流派组合强度

定义：

流派可以由玩家选择的一组高频卡牌组合、团队职业组合，或两者共同构成。例如“高攻速武器流”“范围清怪流”“召唤物流”“坦克续航流”等。

分析方法：

| 方法 | 说明 |
| --- | --- |
| FP-Growth | 挖掘高频卡牌组合 |
| 支持度 | 某组合在所有对局中出现的比例 |
| 置信度 | 选择 A 后同时选择 B 的概率 |
| 提升度 | A 与 B 同时出现是否强于随机共现 |
| 组合表现 | 该组合对应的平均轮次、胜率、击杀效率 |

最小运行要求：

| 项目 | 最小要求 |
| --- | --- |
| 必要数据 | 每局玩家卡牌集合、团队职业集合、对局结果 |
| 最小样本 | 至少 100-500 局对局更适合展示组合规律 |
| 算法要求 | Spark MLlib FP-Growth |
| 输出形式 | 高频组合列表、强势组合排行、组合网络图 |

## 4. 推荐数据表设计

### 4.1 ODS 原始事件表

表名：`ods_game_event_log`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `event_type` | string | 事件类型 |
| `event_time` | timestamp | 事件时间 |
| `match_id` | string | 对局 ID |
| `room_id` | string | 房间 ID |
| `player_id` | string | 玩家 ID |
| `raw_json` | string | 原始事件内容 |
| `dt` | string | 分区日期 |

### 4.2 玩家对局明细宽表

表名：`dws_player_match_summary`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `match_id` | string | 对局 ID |
| `player_id` | string | 玩家 ID |
| `hero_id` | string | 职业 ID |
| `final_wave` | int | 最终轮次 |
| `survival_wave` | int | 玩家存活轮次 |
| `kill_count` | int | 击杀数 |
| `death_grid_id` | string | 死亡网格 |
| `card_list` | array<string> | 已选择卡牌集合 |
| `is_win` | int | 是否胜利 |
| `dt` | string | 分区日期 |

### 4.3 职业平衡分析表

表名：`ads_hero_balance_report`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `hero_id` | string | 职业 ID |
| `use_count` | int | 使用次数 |
| `avg_survival_wave` | double | 平均存活轮次 |
| `avg_kill_per_wave` | double | 每轮平均击杀数 |
| `win_rate` | double | 胜率 |
| `balance_score` | double | 综合平衡评分 |
| `dt` | string | 统计日期 |

### 4.4 地图热力分析表

表名：`ads_map_grid_heatmap_report`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `map_id` | string | 地图 ID |
| `grid_id` | string | 网格 ID |
| `death_count` | int | 死亡次数 |
| `stay_duration` | double | 停留时长 |
| `enemy_spawn_count` | int | 敌人刷新数量 |
| `danger_score` | double | 危险系数 |
| `dt` | string | 统计日期 |

### 4.5 卡牌强度分析表

表名：`ads_card_strength_report`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `card_id` | string | 卡牌 ID |
| `show_count` | int | 出现次数 |
| `pick_count` | int | 选择次数 |
| `pick_rate` | double | 选取率 |
| `win_rate_lift` | double | 胜率提升 |
| `wave_lift` | double | 平均轮次提升 |
| `strength_score` | double | 综合强度评分 |
| `dt` | string | 统计日期 |

## 5. 最小可运行方案

为了保证课程设计可以落地，建议采用“先离线、后实时”的最小可运行方案。

### 5.1 最小技术栈

| 模块 | 最小技术选型 |
| --- | --- |
| 数据生成 | Python 脚本模拟游戏日志 |
| 数据存储 | HDFS + Hive |
| 离线计算 | Spark SQL / PySpark |
| 分析算法 | Spark MLlib FP-Growth |
| 结果存储 | Hive ADS 表 / MySQL |
| 可视化 | Superset / Grafana / 简单 Web 页面 |
| 实时监控 | Flink + Kafka，可作为增强模块 |

### 5.2 最小硬件要求

| 资源 | 最小配置 | 推荐配置 |
| --- | --- | --- |
| CPU | 2 核 | 4 核以上 |
| 内存 | 4GB | 8GB-16GB |
| 磁盘 | 10GB | 30GB 以上 |
| 操作系统 | Windows + WSL / Linux / macOS | Linux 或 WSL2 |
| Java | JDK 8 或 JDK 11 | JDK 8/11，根据 Hadoop 版本选择 |
| Python | Python 3.8+ | Python 3.10+ |

### 5.3 最小软件要求

| 软件 | 最小要求 |
| --- | --- |
| Hadoop | 3.x，伪分布式或单机模式 |
| Hive | 3.x |
| Spark | 3.x |
| Kafka | 3.x，可选 |
| Flink | 1.17+，可选 |
| MySQL | 5.7+ 或 8.x，可选 |
| Superset | 2.x+，可选 |

### 5.4 最小数据要求

| 数据类型 | 最小要求 |
| --- | --- |
| 对局数量 | 100-500 局 |
| 玩家数量 | 20-100 名模拟玩家 |
| 职业数量 | 4-8 个 |
| 卡牌数量 | 30-80 张 |
| 地图数量 | 1-3 张 |
| 位置采样 | 每名玩家每 1-5 秒一条 |
| 敌人刷新数据 | 每轮至少记录刷新点和刷新数量 |

### 5.5 最小功能闭环

最小可运行版本应至少完成以下功能：

1. 生成或采集游戏事件日志。
2. 将日志导入 Hive ODS 表。
3. 使用 Spark 清洗生成 DWD 明细表。
4. 生成玩家对局汇总宽表。
5. 计算职业平衡、地图热力、卡牌强度三类核心指标。
6. 使用 FP-Growth 挖掘职业与卡牌组合。
7. 将结果写入 ADS 表。
8. 使用 Superset、Grafana 或 Web 页面展示结果。

## 6. 项目实现优先级

| 优先级 | 内容 | 说明 |
| --- | --- | --- |
| P0 | 离线日志生成与 Hive 入仓 | 保证系统有数据可分析 |
| P0 | Spark 指标计算 | 完成课程设计核心工作量 |
| P0 | 职业、地图、卡牌三类看板 | 直接对应核心分析内容 |
| P1 | FP-Growth 流派挖掘 | 展示大数据挖掘能力 |
| P1 | 简单 Web 可视化 | 提升展示效果 |
| P2 | Kafka + Flink 实时监控 | 作为系统亮点和扩展模块 |
| P2 | 自动调度与告警 | 增强工程完整性 |

## 7. 课程设计建议交付物

最终建议交付以下内容：

1. 系统技术架构图。
2. 数据采集事件字典。
3. Hive 分层数据表设计。
4. Spark 离线分析任务代码。
5. FP-Growth 流派挖掘结果。
6. 职业平衡、地图热力、卡牌强度可视化看板。
7. 系统运行说明文档。
8. 平衡性分析报告和调参建议。

## 8. 可扩展方向

后续可以继续扩展以下能力：

1. 引入玩家水平分层，降低卡牌和职业强度分析中的玩家技术偏差。
2. 引入版本维度，对比不同版本的平衡性变化。
3. 使用因果推断或倾向得分匹配，进一步分析卡牌真实收益。
4. 构建职业、卡牌、怪物、地图之间的多维分析模型。
5. 增加自动异常检测，发现版本更新后异常强势或异常弱势内容。
