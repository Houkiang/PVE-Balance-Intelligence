# 基于模拟数据生成的离线游戏平衡性分析系统开发规划

## 1. 开发目标

本阶段优先实现一个可运行、可展示、可扩展的离线分析系统。由于课程设计环境中无法真实组织大量玩家同时游玩，系统将使用脚本批量生成游戏对局数据，通过模拟数据完成职业平衡、地图难点区域、卡牌强度和职业流派组合强度分析。

本阶段暂不实现实时计算链路，Kafka、Flink 和实时告警作为后续扩展内容保留在架构设计中。

核心目标如下：

1. 使用脚本批量生成具有业务合理性的游戏事件数据。
2. 使用 HDFS 存储 ODS、DWD、DWS、ADS 各层数据文件。
3. 使用 PySpark 完成核心平衡性指标计算。
4. 使用 FP-Growth 挖掘常见卡牌组合和团队职业组合。
5. 使用 Streamlit 展示分析结果。
6. 形成完整课程设计闭环：数据生成、数据入仓、离线计算、结果展示、分析报告。

## 2. 技术路线

### 2.1 当前阶段技术栈

| 模块 | 技术选型 | 说明 |
| --- | --- | --- |
| 数据生成 | Python | 批量生成模拟游戏事件日志 |
| 原始数据格式 | JSON Lines | 一行一条事件，便于追加生成和 Spark 读取 |
| 分布式存储 | Hadoop HDFS | 作为 ODS、DWD、DWS、ADS 主存储 |
| 分层数据格式 | Parquet | DWD、DWS、ADS 使用 Parquet，便于 Spark 高效读取 |
| 离线计算 | PySpark / Spark SQL | 清洗、聚合、指标计算 |
| 组合挖掘 | Spark MLlib FP-Growth | 挖掘卡牌组合和职业组合 |
| 结果展示 | Streamlit | 读取本地导出的 ADS CSV/Parquet 并展示图表 |
| 报告输出 | Markdown / Word / PDF | 汇总分析结论 |

### 2.2 暂不实现内容

| 模块 | 暂缓原因 |
| --- | --- |
| Kafka 实时接入 | 当前没有真实持续事件流，模拟数据批处理更适合课程进度 |
| Flink 实时计算 | 实时监控不是当前最小闭环必要功能 |
| 告警系统 | 可在最终文档中作为扩展设计描述 |
| Hive 数仓 | 暂不使用 Hive 建表，后续可将 HDFS 上的 Parquet 映射为 Hive 外部表 |
| 复杂玩家 AI | 数据生成阶段只需保证统计规律合理，不需要真实战斗模拟 |

### 2.3 拆分文档

详细开发步骤已拆分到 `docs/` 目录：

| 文档 | 内容 |
| --- | --- |
| `docs/01_数据生成开发步骤.md` | 模拟数据生成、事件设计、配置设计 |
| `docs/02_HDFS数据湖开发步骤.md` | HDFS 分层目录、数据格式、ADS 本地导出 |
| `docs/03_PySpark离线分析开发步骤.md` | DWD、DWS、ADS 任务开发 |
| `docs/04_Streamlit可视化开发步骤.md` | Streamlit 页面、图表和读取方式 |
| `docs/05_指标口径与数据字典.md` | 50 波达成率、高表现率、地图网格等指标口径 |
| `docs/06_配置表设计.md` | 职业、卡牌、武器、地图和发卡规则配置 |

## 3. 推荐项目目录结构

```text
Dashuju/
  docs/
    大数据游戏平衡性分析系统技术架构.md
    开发规划.md
    数据字典.md
    运行说明.md
  config/
    simulation_config.yaml
    card_config.csv
    hero_config.csv
    weapon_config.csv
    map_config.csv
  data/
    local_raw/
    ads_export/
    sample/
  scripts/
    generate_data.py
    upload_to_hdfs.ps1
    upload_to_hdfs.sh
  spark_jobs/
    clean_events.py
    build_player_match_summary.py
    analyze_hero_balance.py
    analyze_map_heatmap.py
    analyze_card_strength.py
    mine_build_combinations.py
  dashboard/
    streamlit_app.py
  reports/
```

说明：

| 目录 | 用途 |
| --- | --- |
| `docs/` | 存放架构、规划、数据字典、运行说明 |
| `config/` | 存放职业、卡牌、地图、模拟参数配置 |
| `data/` | 存放本地调试数据、样例数据和给 Streamlit 使用的 ADS 导出数据，正式分层数据以 HDFS 为准 |
| `scripts/` | 存放通用脚本和 HDFS 上传脚本 |
| `spark_jobs/` | 存放 PySpark 离线分析任务 |
| `dashboard/` | 存放 Streamlit 可视化页面 |
| `reports/` | 存放最终分析报告 |

HDFS 推荐目录结构：

```text
/game_balance/
  ods/game_event_log/dt=yyyy-MM-dd/
  dwd/game_event_detail/dt=yyyy-MM-dd/
  dwd/player_wave_stat_detail/dt=yyyy-MM-dd/
  dwd/card_pick_detail/dt=yyyy-MM-dd/
  dwd/position_tick_detail/dt=yyyy-MM-dd/
  dwd/enemy_spawn_detail/dt=yyyy-MM-dd/
  dws/match_summary/dt=yyyy-MM-dd/
  dws/player_match_summary/dt=yyyy-MM-dd/
  dws/player_card_set/dt=yyyy-MM-dd/
  dws/map_grid_summary/dt=yyyy-MM-dd/
  ads/hero_balance_report/dt=yyyy-MM-dd/
  ads/map_grid_heatmap_report/dt=yyyy-MM-dd/
  ads/card_strength_report/dt=yyyy-MM-dd/
  ads/build_combination_report/dt=yyyy-MM-dd/
```

## 4. 数据生成设计

### 4.1 数据生成目标

模拟数据需要满足三个要求：

1. 规模足够：能够体现大数据课程中的批量处理和统计分析。
2. 规律合理：不同职业、卡牌和地图区域之间要有可解释的强弱差异。
3. 配置驱动：数据生成脚本只执行模拟逻辑，职业、卡牌、武器、地图和发卡规则由配置文件决定。

### 4.2 模拟对象

| 对象 | 示例规模 | 说明 |
| --- | --- | --- |
| 玩家 | 100-1000 名 | 使用模拟玩家 ID |
| 对局 | 500-5000 局 | 每局 2-10 名玩家 |
| 职业 | 4-8 个 | 如战士、法师、游侠、工程师等 |
| 卡牌 | 30-80 张 | 包括武器、属性、召唤、续航等类型 |
| 地图 | 1-3 张 | 每张地图使用 1000x1000 二维网格 |
| 轮次 | 5-80 轮 | 根据队伍强度随机决定最终轮次 |

### 4.3 推荐事件类型

| 事件类型 | 是否必须 | 说明 |
| --- | --- | --- |
| `battle_start` | 必须 | 对局开始 |
| `battle_end` | 必须 | 对局结束 |
| `player_join` | 必须 | 玩家加入对局 |
| `wave_start` | 必须 | 轮次开始 |
| `wave_end` | 可选 | 轮次结束 |
| `enemy_spawn` | 必须 | 敌人刷新 |
| `enemy_kill` | 必须 | 玩家击杀敌人 |
| `player_death` | 必须 | 玩家死亡 |
| `level_up` | 必须 | 玩家升级 |
| `card_choice` | 必须 | 玩家升级时出现的候选卡牌，用于计算卡牌选取率 |
| `card_pick` | 必须 | 玩家选择卡牌 |
| `position_tick` | 必须 | 玩家位置采样 |
| `player_wave_stat` | 必须 | 玩家每轮战斗表现汇总 |

### 4.3.1 配置驱动原则

数据生成脚本不应把核心数值写死在代码中。推荐将脚本定位为“模拟引擎”，将规则放到配置文件中：

| 配置文件 | 决定内容 |
| --- | --- |
| `simulation_config.yaml` | 对局规模、随机种子、50 波目标、队伍人数范围、地图尺寸 |
| `hero_config.csv` | 职业基础属性、定位、输出、生存、治疗、承伤、卡牌偏好 |
| `card_config.csv` | 卡牌类型、出现权重、出现轮次、效果、负面词条、适配职业、流派标签 |
| `weapon_config.csv` | 武器基础数值、攻击方式、升级维度、成长曲线 |
| `map_config.csv` | 地图危险区域、敌人刷新密度、位置分布偏置 |

当前卡牌类型建议固定为：

| 卡牌类型 | 说明 |
| --- | --- |
| `permanent_buff` | 永久属性 Buff，如力量、幸运、移速、防御 |
| `instant_effect` | 短时或即时数值效果，如立刻回血、短时护盾 |
| `new_weapon` | 获得新武器，如法杖、飞刀、火炮 |
| `weapon_upgrade` | 升级已有武器，如伤害、CD、攻击次数、频率、范围 |
| `global_affix` | 后期出现的带负面效果的额外全局属性词条 |

卡牌出现权重建议由多因素决定：

```text
final_weight =
  base_weight
* wave_phase_weight
* hero_affinity_weight
* build_synergy_weight
* prerequisite_weight
* rarity_weight
```

其中 `weapon_upgrade` 必须满足玩家已拥有对应武器，否则 `prerequisite_weight = 0`；`global_affix` 只在后期出现，例如 `wave >= 35`。

### 4.4 事件公共字段

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `event_id` | string | 事件唯一 ID |
| `event_type` | string | 事件类型 |
| `event_time` | string | 事件时间 |
| `dt` | string | 分区日期 |
| `match_id` | string | 对局 ID |
| `room_id` | string | 房间 ID |
| `map_id` | string | 地图 ID |
| `player_id` | string | 玩家 ID，可为空 |
| `hero_id` | string | 职业 ID，可为空 |
| `wave` | int | 当前轮次 |
| `x` | double | 地图 X 坐标，可为空 |
| `y` | double | 地图 Y 坐标，可为空 |
| `extra` | string | JSON 字符串，存放事件扩展字段 |

地图坐标约定：

| 项目 | 约定 |
| --- | --- |
| 地图宽度 | 1000 |
| 地图高度 | 1000 |
| X 坐标范围 | 0-999 |
| Y 坐标范围 | 0-999 |
| 网格 ID | `map_id + '_' + grid_x + '_' + grid_y` |
| 存储方式 | 只存储有事件发生的稀疏网格 |

`player_wave_stat` 扩展字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `damage_dealt` | double | 本轮造成伤害 |
| `kill_count` | int | 本轮击杀数 |
| `heal_done` | double | 本轮治疗量 |
| `damage_taken` | double | 本轮受到伤害，坦克可视为承伤贡献，脆皮需要结合生存情况判断 |
| `support_score` | double | 可选，护盾、控制、增益等辅助贡献 |

### 4.5 模拟规则建议

为避免数据完全随机导致分析结果没有意义，数据生成脚本应内置一些可解释的偏置。

| 规则 | 示例 |
| --- | --- |
| 职业强弱差异 | 法师击杀高但生存低，战士生存高但击杀中等 |
| 卡牌强弱差异 | 由 `card_config.csv` 的权重、效果、职业适配和流派标签控制 |
| 武器成长差异 | 由 `weapon_config.csv` 控制伤害、CD、攻击频率、范围等成长曲线 |
| 地图危险区域 | 在 1000x1000 网格中预设若干高危险区域，提高死亡率和敌人刷新密度 |
| 职业卡牌适配 | 法师更适合范围伤害卡，游侠更适合攻速暴击卡 |
| 团队组合差异 | 坦克 + 法师 + 辅助组合平均轮次更高 |
| 轮次难度递增 | 轮次越高，敌人刷新数量和死亡概率越高 |

卡牌选择不采用完全随机。候选卡牌生成后，为每张卡计算选择评分：

```text
pick_score =
  base_card_value
+ hero_fit_score
+ build_synergy_score
+ survival_need_score
+ weapon_growth_score
- risk_penalty
+ random_noise
```

这样能够让自动战斗游戏中的“升级选卡”更像真实玩家决策：血量低时更可能选择回血或防御，已有武器时更可能选择对应升级，后期才更可能选择带负面词条的高收益卡。

### 4.6 数据生成脚本最小功能

`generate_data.py` 应至少支持以下参数：

```bash
python scripts/generate_data.py --matches 1000 --players 300 --output data/local_raw/game_event_log/dt=2026-05-12/events.jsonl
```

参数说明：

| 参数 | 说明 |
| --- | --- |
| `--matches` | 生成对局数量 |
| `--players` | 模拟玩家池大小 |
| `--seed` | 随机种子，保证可复现 |
| `--start-date` | 模拟数据开始日期 |
| `--days` | 生成多少天的数据 |
| `--output` | 输出目录或文件 |

验收标准：

1. 能生成 JSON Lines 格式事件数据。
2. 每局至少包含开始、玩家加入、轮次、击杀、卡牌选择、位置、死亡或结束事件。
3. 数据中能够体现不同职业、卡牌和地图区域的统计差异。
4. 同一随机种子下生成结果可复现。

## 5. HDFS 离线分层开发规划

### 5.1 ODS 层

目标：

保存原始事件数据，不做复杂清洗，保证数据可追溯。

主要任务：

| 任务 | 输出 |
| --- | --- |
| 生成原始事件文件 | `game_event_log` JSON Lines |
| 上传原始数据到 HDFS | `/game_balance/ods/game_event_log/dt=yyyy-MM-dd/` |
| 保留原始 `extra` 字段 | 便于后续扩展 |

验收标准：

1. 能通过 HDFS 命令查看原始事件文件。
2. PySpark 能按 `dt` 目录读取指定日期数据。
3. 原始字段没有明显丢失。

### 5.2 DWD 层

目标：

将原始事件拆解为更适合分析的标准化明细表。

主要任务：

| 任务 | 输出 |
| --- | --- |
| 清洗事件公共字段 | `dwd_game_event_detail` |
| 拆解玩家事件 | `dwd_player_event_detail` |
| 拆解卡牌选择事件 | `dwd_card_pick_detail` |
| 拆解位置采样事件 | `dwd_position_tick_detail` |
| 拆解敌人刷新事件 | `dwd_enemy_spawn_detail` |
| 拆解玩家每轮表现事件 | `dwd_player_wave_stat_detail` |

验收标准：

1. 事件类型、时间、玩家、职业、轮次字段可直接查询。
2. 卡牌 ID、位置坐标、敌人数量等扩展字段被解析出来。
3. 造成伤害、击杀数、治疗量、承伤等玩家表现字段被解析出来。
4. 脏数据或缺失数据有基础过滤逻辑。

### 5.3 DWS 层

目标：

构建对局级、玩家级、卡牌级、地图网格级汇总宽表。

主要任务：

| 任务 | 输出 |
| --- | --- |
| 对局汇总 | `dws_match_summary` |
| 玩家对局汇总 | `dws_player_match_summary` |
| 玩家卡牌集合汇总 | `dws_player_card_set` |
| 地图网格汇总 | `dws_map_grid_summary` |

验收标准：

1. 每名玩家每局有一条汇总记录。
2. 可以查询每局最终轮次、是否达成 50 波、玩家击杀、伤害、治疗、承伤、死亡位置、卡牌集合。
3. 地图网格能统计死亡次数、停留时长和敌人刷新数量。

### 5.4 ADS 层

目标：

生成直接服务于展示和报告的分析结果表。

主要任务：

| 任务 | 输出 |
| --- | --- |
| 职业平衡分析 | `ads_hero_balance_report` |
| 地图热力分析 | `ads_map_grid_heatmap_report` |
| 卡牌强度分析 | `ads_card_strength_report` |
| 流派组合分析 | `ads_build_combination_report` |
| 本地展示数据导出 | `data/ads_export/` |

验收标准：

1. 每张 ADS 表都能直接用于图表展示。
2. 指标字段含义明确，能支撑课程答辩说明。
3. 至少输出 TopN 异常职业、危险区域、强势卡牌和强势组合。
4. PySpark 能将 HDFS 上的 ADS 结果导出为本地 CSV/Parquet，供 Streamlit 读取。

## 6. 核心分析任务规划

### 6.1 职业平衡性分析

输入：

`dws_player_match_summary`

输出：

`ads_hero_balance_report`

指标：

| 指标 | 说明 |
| --- | --- |
| 平均存活轮次 | 判断职业生存能力 |
| 每轮平均击杀数 | 判断职业输出能力 |
| 出场率 | 判断职业受欢迎程度 |
| 50 波达成率 | 该职业参与的队伍达成 50 波的比例 |
| 高表现率 | 该职业玩家获得高表现评价的比例 |
| 平衡评分 | 综合评价职业强度 |

胜率定义：

本项目暂不使用传统通关胜率。由于游戏是无限轮次 PVE，胜率定义为“全队达成 50 波”的比例：

```text
match_success = final_wave >= 50
hero_success_rate = 使用该职业的玩家所在队伍达成 50 波的次数 / 使用该职业的玩家出场次数
```

玩家高表现率定义：

高表现率不是单纯判断最终轮次是否进入前 25%，而是综合玩家在对局中的贡献。建议先对以下指标做职业内标准化，再计算综合表现分：

```text
performance_score =
  0.35 * normalized_damage_dealt
+ 0.25 * normalized_kill_count
+ 0.20 * normalized_heal_done
+ 0.20 * role_survival_adjusted_tank_score
```

`role_survival_adjusted_tank_score` 用于处理承伤口径：坦克职业将有效承伤视为贡献，但需要结合存活轮次或存活率，避免把无效挨打误判为高贡献；脆皮职业承伤过高通常视为风险，可通过存活率修正或降低权重。同一职业内 `performance_score` 进入前 25% 的玩家记为高表现玩家。职业内标准化可以减少不同职业定位差异带来的偏差。

验收标准：

1. 能输出职业强度排行榜。
2. 能解释每个职业强或弱的原因。
3. 能识别明显过强或过弱职业。

### 6.2 地图难点区域分析

输入：

`dwd_position_tick_detail`、`dwd_player_event_detail`、`dwd_enemy_spawn_detail`

输出：

`ads_map_grid_heatmap_report`

指标：

| 指标 | 说明 |
| --- | --- |
| 死亡次数 | 玩家在该网格死亡次数 |
| 停留时长 | 玩家在该网格累计停留时间 |
| 敌人刷新数量 | 敌人在该网格刷新总量 |
| 危险系数 | 综合衡量区域难度 |

地图网格规则：

```text
grid_x = floor(x)
grid_y = floor(y)
grid_id = concat(map_id, '_', grid_x, '_', grid_y)
```

地图为 1000x1000 二维网格，坐标范围为 `0 <= x < 1000`、`0 <= y < 1000`。ADS 层只输出有玩家停留、死亡或敌人刷新的网格，Streamlit 展示时可按需要对 1000x1000 网格进行降采样或区域聚合。

验收标准：

1. 能按照地图网格输出危险区域 TopN。
2. 能生成热力图需要的数据。
3. 能解释危险区域与死亡、刷新、停留之间的关系。

### 6.3 卡牌强度分析

输入：

`dwd_card_pick_detail`、`dws_player_match_summary`

输出：

`ads_card_strength_report`

指标：

| 指标 | 说明 |
| --- | --- |
| 选取率 | 玩家看到卡牌后选择的概率 |
| 携带后 50 波达成率提升 | 携带该卡牌与未携带该卡牌的 50 波达成率差 |
| 携带后平均轮次提升 | 携带该卡牌与未携带该卡牌的平均轮次差 |
| 成长曲线 | 武器升级卡牌等级与数值变化 |

验收标准：

1. 能输出卡牌强度排行榜。
2. 能区分高选取率和高收益卡牌。
3. 能展示至少一类武器升级卡牌成长曲线。

### 6.4 职业与流派组合强度分析

输入：

`dws_player_card_set`、`dws_match_summary`

输出：

`ads_build_combination_report`

方法：

使用 FP-Growth 对卡牌集合和团队职业集合进行关联规则挖掘。

指标：

| 指标 | 说明 |
| --- | --- |
| 支持度 | 组合出现频率 |
| 置信度 | 组合内部关联强度 |
| 提升度 | 组合是否显著高于随机共现 |
| 平均最终轮次 | 组合对应的平均表现 |
| 50 波达成率 | 组合对应的队伍达成 50 波比例 |

验收标准：

1. 能输出常见卡牌组合。
2. 能输出常见团队职业组合。
3. 能识别高强度流派组合并给出解释。

## 7. 开发阶段安排

### 阶段一：数据生成与配置建模

目标：

完成可复现的模拟数据生成脚本。

任务：

| 编号 | 任务 | 产物 |
| --- | --- | --- |
| 1.1 | 设计职业配置 | `config/hero_config.csv` |
| 1.2 | 设计卡牌配置 | `config/card_config.csv` |
| 1.3 | 设计地图配置 | `config/map_config.csv` |
| 1.4 | 实现数据生成脚本 | `scripts/generate_data.py` |
| 1.5 | 生成样例数据 | `data/sample/` |

验收：

能生成至少 1000 局对局事件数据，并能用脚本重复生成相同结果。

### 阶段二：HDFS 分层落盘与数据入湖

目标：

建立基于 HDFS + Parquet 的基础离线数据湖。

任务：

| 编号 | 任务 | 产物 |
| --- | --- | --- |
| 2.1 | 上传 ODS 原始数据 | `/game_balance/ods/game_event_log/` |
| 2.2 | 生成 DWD Parquet 明细 | `/game_balance/dwd/` |
| 2.3 | 生成 DWS Parquet 汇总 | `/game_balance/dws/` |
| 2.4 | 生成 ADS Parquet 结果 | `/game_balance/ads/` |
| 2.5 | 导出 ADS 到本地展示目录 | `data/ads_export/` |
| 2.6 | 编写 HDFS 上传脚本 | `scripts/upload_to_hdfs.ps1`、`scripts/upload_to_hdfs.sh` |

验收：

HDFS 中可以查看 ODS、DWD、DWS、ADS 各层数据，PySpark 可以直接读取和写入对应目录，并能将 ADS 结果导出为本地 CSV/Parquet。

### 阶段三：Spark 离线分析任务

目标：

完成核心分析指标计算。

任务：

| 编号 | 任务 | 产物 |
| --- | --- | --- |
| 3.1 | 事件清洗任务 | `spark_jobs/clean_events.py` |
| 3.2 | 玩家对局汇总任务 | `spark_jobs/build_player_match_summary.py` |
| 3.3 | 职业平衡分析任务 | `spark_jobs/analyze_hero_balance.py` |
| 3.4 | 地图热力分析任务 | `spark_jobs/analyze_map_heatmap.py` |
| 3.5 | 卡牌强度分析任务 | `spark_jobs/analyze_card_strength.py` |
| 3.6 | 流派组合挖掘任务 | `spark_jobs/mine_build_combinations.py` |

验收：

能生成四类 ADS 分析结果表。

### 阶段四：可视化与报告

目标：

将分析结果展示为课程答辩可用的图表和结论。

任务：

| 编号 | 任务 | 产物 |
| --- | --- | --- |
| 4.1 | 职业平衡看板 | 职业强度排行、雷达图或柱状图 |
| 4.2 | 地图热力看板 | 网格热力图、危险区域 TopN |
| 4.3 | 卡牌强度看板 | 卡牌选取率、50 波达成率提升、轮次提升 |
| 4.4 | 流派组合看板 | 高频组合、强势组合 |
| 4.5 | 编写分析报告 | `reports/平衡性分析报告.md` |

验收：

能够通过图表解释系统发现的问题，并给出平衡性调整建议。

## 8. 里程碑计划

| 里程碑 | 内容 | 建议耗时 |
| --- | --- | --- |
| M1 | 完成数据生成脚本和样例数据 | 2-3 天 |
| M2 | 完成 HDFS 目录规划和数据上传 | 1-2 天 |
| M3 | 完成 PySpark 清洗与汇总任务 | 3-5 天 |
| M4 | 完成四类分析指标 | 3-5 天 |
| M5 | 完成 Streamlit 可视化与报告 | 2-4 天 |

总耗时建议：

约 2-3 周完成最小可运行版本。如果课程时间较紧，可以优先完成 M1、M3 和 M5，先用本地文件调试，再上传到 HDFS 跑完整链路。

## 9. 风险与应对

| 风险 | 影响 | 应对 |
| --- | --- | --- |
| Hadoop 环境部署耗时 | 影响开发进度 | 先使用本地 JSONL/Parquet + Spark Local 模式完成分析，再迁移到 HDFS |
| 模拟数据过于随机 | 分析结果不可解释 | 在配置中预设职业、卡牌、地图强弱偏置 |
| 指标过多导致实现压力大 | 无法按时完成 | 优先实现职业、地图、卡牌三类核心指标 |
| FP-Growth 调参困难 | 组合结果太多或太少 | 调整最小支持度和最小置信度 |
| 1000x1000 热力图过大 | Streamlit 展示卡顿 | ADS 层保留稀疏网格，展示层按区域聚合或降采样 |
| 可视化工具部署困难 | 展示效果受限 | 使用 Streamlit 读取本地 `data/ads_export/` 下的 ADS 导出文件 |

## 10. 最小可交付版本

如果时间有限，最小可交付版本应包含：

1. `generate_data.py`：生成模拟游戏事件数据。
2. `clean_events.py`：清洗原始事件。
3. `build_player_match_summary.py`：生成玩家对局汇总。
4. `analyze_hero_balance.py`：职业平衡分析。
5. `analyze_map_heatmap.py`：地图难点区域分析。
6. `analyze_card_strength.py`：卡牌强度分析。
7. `mine_build_combinations.py`：职业和卡牌组合挖掘。
8. `dashboard/streamlit_app.py`：Streamlit 可视化看板。
9. 至少 4 张 ADS 结果文件。
10. 至少 4 类图表。
11. 一份平衡性分析报告。

## 11. 推荐开发顺序

建议按以下顺序开发：

1. 先写配置文件，明确职业、卡牌、地图的模拟规则。
2. 再写数据生成脚本，保证数据规模和业务规律。
3. 使用小样本数据验证事件结构。
4. 编写清洗任务，将事件转换为标准明细。
5. 构建玩家对局汇总宽表。
6. 先实现职业平衡分析，因为它最容易验证。
7. 再实现地图热力和卡牌强度分析。
8. 最后实现 FP-Growth 组合挖掘。
9. 将结果接入 Streamlit 可视化。
10. 编写最终报告和答辩说明。

## 12. 当前阶段结论

当前阶段的最优开发策略是先放弃实时链路和 Hive 建表，集中完成“Python 模拟数据生成 + HDFS 分布式存储 + PySpark 离线分析 + 本地 ADS 导出 + Streamlit 可视化”的闭环。这样既符合课程设计可落地性的要求，也能体现大数据系统的核心能力：批量数据处理、分布式存储、离线指标计算、关联规则挖掘和可视化分析。
